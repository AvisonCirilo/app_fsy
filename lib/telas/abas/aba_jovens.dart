import 'package:flutter/material.dart';

class AbaJovens extends StatelessWidget {
  const AbaJovens({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Aqui ficará o Feed de Avisos', 
        style: TextStyle(fontSize: 18),
      ),
    );
  }
}